# MOD-01 — Mac / Windows geliştirme ortamı bağımlılıkları
# (Pi için requirements.txt kullan)
#
# Kurulum:
#   pip install -r requirements-dev.txt

onnxruntime>=1.17.0
opencv-python>=4.7.0        # headless değil — cv2.imshow (önizleme) için
paho-mqtt>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0
